<!DOCTYPE html>
<html>
<head>
  <title>Add Comment</title>
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.16/dist/tailwind.min.css" rel="stylesheet">
</head>
<body>
<div class="min-h-screen w-full flex flex-col justify-center items-center bg-gray-100">
  <h1 class="text-2xl font-semibold mb-4">Add a Comment</h1>

  <%= form_with model: @comment, url: user_post_comments_path(@user, @post), class: "mb-4" do |f| %>
    <%= f.text_area :text, placeholder: "Add a comment...", class: "w-full border rounded-lg p-2" %>
    <%= f.submit 'Add Comment', class: "px-4 py-2 bg-blue-500 text-white rounded-lg hover:bg-blue-600 mt-2", data: { disable_with: 'Adding Comment...' } %>
  <% end %>

</div>
</body>
</html>
