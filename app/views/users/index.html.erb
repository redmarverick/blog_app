<!DOCTYPE html>
<html>
<head>
  <title>List of Users</title>
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.16/dist/tailwind.min.css" rel="stylesheet">
</head>
<body>
<div class="min-h-screen flex justify-center items-center bg-gray-100">
  <ul class="space-y-4">
    <% @users.each do |user| %>
      <li class="flex items-center space-x-4 p-4 bg-white border border-gray-300 rounded-lg">
        <div class="w-12 h-12">
          <img src="<%= user.photo %>" class="w-full h-full rounded-full">
        </div>
        <div class="flex flex-col">
          <a href="<%= user_path(user) %>" class="text-blue-500 hover:underline">
            <div class="text-lg font-semibold"><%= user.name %></div>
          </a>
          <div class="text-gray-600">Posts: <%= user.posts_counter %></div>
        </div>
      </li>
    <% end %>
  </ul>
</div>
</body>
</html>
