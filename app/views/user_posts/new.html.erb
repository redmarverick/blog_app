<!DOCTYPE html>
<html>
<head>
  <title>New Post</title>
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.16/dist/tailwind.min.css" rel="stylesheet">
</head>
<body>
<div class="min-h-screen w-full flex flex-col justify-center items-center bg-gray-100">
  <%= form_for [@user, @post] do |f| %>
    <div class="w-10/12 bg-white border border-gray-300 rounded-lg p-4">
      <%= f.label :title %>
      <%= f.text_field :title, class: 'w-full border rounded-lg p-2 mt-2' %>

      <%= f.label :text %>
      <%= f.text_area :text, class: 'w-full border rounded-lg p-2 mt-2' %>

      <%= f.submit 'Create Post', class: 'px-4 py-2 bg-green-500 text-white rounded-lg hover:bg-green-600 mt-2' %>
    </div>
  <% end %>
</div>
</body>
</html>